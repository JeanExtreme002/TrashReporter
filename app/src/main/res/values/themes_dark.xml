<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Dark Theme específico para o app -->
    <style name="Theme.TrashReporter.Dark" parent="Theme.Material3.Dark.NoActionBar">
        <!-- Primary colors for dark theme -->
        <item name="colorPrimary">@color/button_primary_dark_gradient_start</item>
        <item name="colorPrimaryVariant">@color/button_primary_dark_gradient_end</item>
        <item name="colorSecondary">@color/accent_orange</item>
        <item name="colorSecondaryVariant">@color/accent_green</item>
        
        <!-- Background colors for dark theme -->
        <item name="android:colorBackground">@color/background_dark</item>
        <item name="colorSurface">@color/surface_dark</item>
        
        <!-- Text colors for dark theme -->
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="colorOnBackground">@color/text_primary_dark</item>
        <item name="colorOnSurface">@color/text_primary_dark</item>
        
        <!-- Status bar for dark theme -->
        <item name="android:statusBarColor">@color/background_dark</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:navigationBarColor">@color/navigation_background_dark</item>
        
        <!-- Force dark mode -->
        <item name="android:forceDarkAllowed" tools:targetApi="q">false</item>
    </style>
</resources>
